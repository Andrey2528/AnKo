# Інструкція по переходу на Firebase + GitHub

## 📋 Що було зроблено

Ваша адмін-панель тепер підтримує **два режими роботи**:

1. **localStorage** (старий) - дані зберігаються локально в браузері
2. **Firebase + GitHub** (новий) - дані в хмарі, зображення на GitHub

## 🎯 Чому це круто?

### Firebase Firestore
- ✅ Доступ до адмінки з будь-якого пристрою
- ✅ Дані не зникнуть при очищенні кешу браузера
- ✅ Реальна база даних з запитами
- ✅ Автоматична синхронізація
- ✅ Безкоштовно до 50K reads/day

### GitHub для зображень
- ✅ Безкоштовне зберігання
- ✅ Версіонування (історія змін)
- ✅ CDN від GitHub (швидка доставка)
- ✅ Backup в Git репозиторії

## 🚀 Швидкий старт

### Крок 1: Створіть Firebase проект

1. Перейдіть на https://console.firebase.google.com/
2. Натисніть "Add project" (Додати проект)
3. Назвіть проект, наприклад: **AnKo-Portfolio**
4. Вимкніть Google Analytics (не обов'язково)
5. Натисніть "Create project"

### Крок 2: Увімкніть Firestore

1. У меню зліва: **Build** → **Firestore Database**
2. Натисніть **"Create database"**
3. Оберіть **"Start in production mode"**
4. Оберіть регіон (найближчий до вас, наприклад `europe-west1`)
5. Натисніть **"Enable"**

### Крок 3: Налаштуйте правила безпеки

1. Перейдіть до **Firestore Database** → **Rules**
2. Вставте такі правила:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Всі можуть читати (для сайту)
    match /{document=**} {
      allow read: if true;
    }
    
    // Тільки ви можете редагувати (поки що без авторизації)
    match /{document=**} {
      allow write: if true;  // Змініть на auth після налаштування
    }
  }
}
```

3. Натисніть **"Publish"**

### Крок 4: Отримайте конфігурацію Firebase

1. Перейдіть до **Project Settings** (іконка шестерінки) → **General**
2. Прокрутіть до "Your apps"
3. Натисніть іконку веба `</>`
4. Зареєструйте застосунок:
   - App nickname: **AnKo Web**
   - НЕ вмикайте Firebase Hosting
5. Скопіюйте значення з `firebaseConfig`:

```javascript
const firebaseConfig = {
  apiKey: "AIzaSy...",           // ← скопіюйте це
  authDomain: "anko-...com",     // ← і це
  projectId: "anko-...",         // ← і це
  storageBucket: "anko-...com",  // ← і це
  messagingSenderId: "123...",   // ← і це
  appId: "1:123..."              // ← і це
};
```

### Крок 5: GitHub Token

1. Перейдіть на https://github.com/settings/tokens
2. Натисніть **"Generate new token"** → **"Generate new token (classic)"**
3. Налаштування:
   - **Note**: `AnKo Image Uploads`
   - **Expiration**: No expiration (або на ваш розсуд)
   - **Scopes**: поставте галочку на **`repo`** (Full control)
4. Натисніть **"Generate token"**
5. **⚠️ ВАЖЛИВО**: Скопіюйте токен ЗАРАЗ! Він виглядає як `ghp_xxxxxxxxxxxx`

### Крок 6: Створіть .env файл

1. У корені проекту створіть файл `.env`:

```bash
# Firebase
VITE_FIREBASE_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
VITE_FIREBASE_AUTH_DOMAIN=anko-portfolio.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=anko-portfolio
VITE_FIREBASE_STORAGE_BUCKET=anko-portfolio.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abcdef123456

# GitHub
VITE_GITHUB_OWNER=Andrey2528
VITE_GITHUB_REPO=AnKo
VITE_GITHUB_BRANCH=main
VITE_GITHUB_TOKEN=ghp_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
```

2. Вставте ваші реальні значення
3. Збережіть файл

### Крок 7: Створіть директорію для зображень

```bash
mkdir -p src/assets/images/uploads
touch src/assets/images/uploads/.gitkeep
git add src/assets/images/uploads/.gitkeep
git commit -m "Add uploads directory"
git push
```

### Крок 8: Перезапустіть dev server

```bash
npm run dev
```

**⚠️ Обов'язково перезапустіть** - змінні з .env завантажуються тільки при старті!

## 🔄 Міграція даних

### Варіант 1: Через адмін-панель (рекомендовано)

1. Відкрийте http://localhost:5173/admin
2. Перейдіть на вкладку **"Migration"** (нова вкладка в сайдбарі)
3. Перевірте що дані є в localStorage (зелені галочки)
4. Натисніть **"🚀 Migrate to Firestore"**
5. Дочекайтесь повідомлення "Migration completed successfully"
6. Перевірте дані на інших вкладках (Home, Pages, Projects)
7. Натисніть **"🗑️ Clear Old LocalStorage"** (опціонально)

### Варіант 2: Через консоль браузера

1. Відкрийте сайт
2. Натисніть F12 → Console
3. Вставте:

```javascript
import { migrateFromLocalStorage } from './src/utils/migration.js';
await migrateFromLocalStorage();
```

## ✅ Перевірка роботи

### 1. Firestore

1. Перейдіть до Firebase Console → Firestore Database
2. Ви повинні побачити колекції:
   - `homeSections` (1 документ з усіма секціями)
   - `projects` (ваші проекти)
   - `pages` (ваші сторінки)

### 2. Завантаження зображень

1. У будь-якій формі адмінки знайдіть компонент завантаження зображення
2. Натисніть "Upload Image"
3. Оберіть картинку (макс 5MB)
4. Дочекайтесь завантаження
5. Перейдіть у ваш GitHub репозиторій → `src/assets/images/uploads/`
6. Ви побачите нову картинку з timestamp у назві

### 3. Перегляд на GitHub

URL завантаженого зображення:
```
https://raw.githubusercontent.com/Andrey2528/AnKo/main/src/assets/images/uploads/1700000000_image.jpg
```

## 🔐 Безпека

### Зараз (тимчасово)
- ❌ Будь-хто може редагувати через адмінку
- ⚠️ НЕ викладайте .env на GitHub!

### Наступний крок: Firebase Authentication

Після того як переконаєтесь що все працює, додамо авторизацію:

1. Firebase Console → **Authentication** → **Get started**
2. Увімкніть **Email/Password**
3. Додайте свій email і пароль
4. Оновіть Firestore rules:

```javascript
match /{document=**} {
  allow read: if true;
  allow write: if request.auth != null;  // тільки авторизовані
}
```

## 🐛 Типові проблеми

### "GitHub token not configured"
- Перевірте що `.env` файл існує
- Перевірте що `VITE_GITHUB_TOKEN` встановлений
- **Перезапустіть dev server** (`npm run dev`)

### "Firebase: Error (auth/invalid-api-key)"
- Перевірте Firebase конфігурацію в `.env`
- Перевірте що проект активний у Firebase Console
- Перезапустіть dev server

### "Upload failed: 404"
- Перевірте що директорія `src/assets/images/uploads` існує в GitHub
- Перевірте VITE_GITHUB_OWNER і VITE_GITHUB_REPO
- Перевірте що токен має права `repo`

### "Permission denied" у Firestore
- Перевірте правила безпеки (Rules)
- Тимчасово поставте `allow write: if true;`

## 📊 Структура даних

### Firestore Collections

**homeSections** (document: content)
```json
{
  "hero": { "title": "...", "buttonText": "...", "buttonLink": "..." },
  "about": { "title": "...", "mainText": "...", ... },
  "services": { "title": "...", "items": [...] },
  "contact": { "title": "...", ... },
  "work": { "marqueeText": "...", "title": "..." }
}
```

**projects** (collection)
```json
{
  "id": "auto-generated",
  "title": "Project Name",
  "description": "...",
  "image": "https://raw.githubusercontent.com/.../image.jpg",
  "order": 0,
  "createdAt": Timestamp,
  "updatedAt": Timestamp
}
```

**pages** (collection)
```json
{
  "id": "auto-generated",
  "slug": "about-us",
  "title": "About Us",
  "content": "...",
  "published": true,
  "createdAt": Timestamp,
  "updatedAt": Timestamp
}
```

## 🎉 Готово!

Тепер ваша адмін-панель:
- 💾 Зберігає дані в Firebase Firestore
- 🖼️ Завантажує зображення на GitHub
- 🌐 Доступна з будь-якого пристрою
- 🔄 Синхронізується в реальному часі

## 📞 Допомога

Якщо щось не працює:
1. Перевірте консоль браузера (F12 → Console)
2. Перевірте .env файл
3. Перезапустіть dev server
4. Перевірте Firebase і GitHub налаштування

Детальна інструкція: `FIREBASE_SETUP.md`
